node_modules/
.wrangler/
.dev.vars
.DS_Store

# Regenerated by each package's build step.
error-pages/dist/

# .env here is branding, not secrets, and Cloudflare only builds what is
# committed - so it belongs in the repository. See error-pages/README.md.
